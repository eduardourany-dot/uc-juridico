{
  "name": "uc-juridico-functions",
  "description": "Cloud Functions for UC Jurídico — DJEN cron + helpers",
  "private": true,
  "scripts": {
    "lint": "echo 'sem lint configurado'",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log --only djenAutoCheckCron"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {}
}
